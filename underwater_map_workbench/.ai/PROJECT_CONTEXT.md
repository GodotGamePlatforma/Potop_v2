# Project Context — mapa podwodna

Rola pliku: krótka, datowana migawka bieżącego pakietu mapy, luk i ostatniej weryfikacji. Rutynowy agent czyta tylko sekcję lub punkt związany ze swoim zadaniem. Reguły trwałe należą do `.ai/DECISIONS.md`, a proces i komendy do `AGENTS.md` oraz `README.md`.

## Stan odniesienia — 2026-09-01

- `map_manifest.json` używa schema v6 i jest jedynym authority rejestracji, globalnego placementu oraz kompozycji `UnderwaterMap`. Świat ma `23040 × 12960` jednostek, a lokalna `UnderwaterMap.tscn` jest wyłącznie deterministyczną pochodną buildera.
- Aktywna kompozycja zawiera proceduralne tło wody L00, natywne bitmapy 1:1 na planach L01/L02, globalną topologię i grunt L05 oraz stabilne korzenie L00–L10. L01/L02 korzystają z asynchronicznej rezydencji okna kamery z budżetem, prefetch, retencją i telemetrią; warstwy wizualne nie posiadają fizyki, a ograniczenie ruchu wynika wyłącznie z topologii.
- Payload L05 schema v2 opisuje jeden globalny raster ownerów. Builder rozdziela granice `world` i zarejestrowanych pakietów struktur bez podwójnej ściany na styku; globalna fizyka świata oraz lokalne kolidery pakietów pozostają osobnymi właścicielami.
- Rejestr zawiera obecnie dwa aktywne pakiety struktur. Mapa posiada ich stable ID, origin, aktywność i hash-pinned referencje. Lokalne manifesty pakietów posiadają rozmiar, topologię, sockety, grafikę, runtime i testy, a ich prywatne ID, pozycje i pozostałe wartości celowo nie są kopiowane do tej ogólnej migawki.
- W01 jest przypięty do sealed manifestu `f3791ecc…f3a13`. Produkcyjna L04 została zbudowana od pustego natywnego rastra `2240 × 3680` jako wyłącznie open-water, bez użycia poprzedniego RGB i bez resize'u lub resamplingu; alfa nadal odpowiada kanonicznej masce piksel w piksel. L05 używa przebudowanej bitmapy konstrukcji w spójnym miejskim stylu. Mapowa `presentation_revision` wynosi `drowned-world-s-loop-depth-v28`; aktywna kompozycja zawiera 10 oszczędnie rozmieszczonych bindingów L01, 10 unikalnych bindingów L02 i jeden globalny binding L05. Warstwy tła mają ciemniejszą hierarchię navy/teal, regiony odrębne akcenty teal/green/rust, a istniejąca mostly-intact wieża `l02_city_16` pozostaje nieblokującą dominantą bez wejścia, platformy ani fizyki. Landmarki L03 są neutralnymi, world-locked adnotacjami z nazwą czytelną dla człowieka oraz akcentem regionu; nie publikują fizyki, nie przechwytują inputu i nie pokazują stable ID.
- Builder, kompilator i smoke odkrywają pakiety dynamicznie. Wspólna warstwa zna tylko publiczną kopertę pakietu, deklarowany skrypt kontrolera oraz cykl `configure(...)` / `reset_attempt()`; prywatny gameplay budynku pozostaje nieprzezroczysty.
- Mapowy visual survey jest manifest-driven i wiąże plan z hashowanym snapshotem przechodnich zależności renderu. Obejmuje każdy landmark, oba kierunki każdego otworu aktywnej struktury, pełne pasma pionowe, dynamicznie wykryte luki tła oraz world-locked, kafelkowy overview; kadry wnętrza i stanów prywatnego runtime nadal należą do danego pakietu.
- `PortalBackdropClearances` w kontrakcie v4 tworzy dla każdego anonimowo wykrytego, traversable biegu otworu granicznego jedną wizualną szczelinę w tle nad L01/L02 i pod L03/L04. Geometria i digest nie zależą od ID, nazwy ani ścieżki pakietu; rdzeń i ograniczony feather używają pełnych kolorów wyprowadzonych z `visual.water_color`, a zewnętrzny tint `0.98` usuwa pozorny ciemny obrys bez zmiany geometrii. Węzły nie mają kolizji, `Area2D`, stanu ani wpływu na topologię.
- L05 ma pierwszy mapowy pass prezentacyjny: maska szczegółu rozdziela osad, szwy ownerów i zużycie ram wejść, a shader nakłada stonowaną wariację materiału i tłumienie wraz z głębokością bez zmiany topologii ani sygnatury gameplayu.
- Globalna L05 prowadzi gracza rzeczywistą, fizycznie wyciętą pętlą S od tutorialu przez Archive i R3 do rozdzielonych tras C4 safe/risk oraz nieobowiązkowej niecki salvage. Dwa skróty, dwa rozłączne prądy, omijalne noise eels, ratunek Leona, ciężki obiekt, kotwice boi i oszczędny loot korzystają wyłącznie z istniejących kolekcji schema v6. Kolejność urządzeń kampanii i stable ID pozostały niezmienione; semantycznie nowa topologia/content zmieniły sygnaturę gameplayu i zgodnie z clean-break odrzucają stare kampanie.
- Avatar gracza jest zewnętrznym konsumentem mapy. Jego scena, grafika, animacje, sockety, VFX i authoring znajdują się w `../diver_workbench/`; złożenie Mapy z Nurkiem oraz regułami kampanii sprawdzają testy integracyjne root.

## Granica pracy

- Pakiety struktur, Mapa i Nurek mogą powstawać równolegle w osobnych worktrees i na osobnych gałęziach. Prywatne `--build-structure` i `--check-structure` pracują tylko na wskazanym ID i lokalnym `.godot`; nie zapisują authority Mapy ani innego pakietu.
- Zmiana struktury wymagająca nowego seala i pinu jest jednym root-routed PR obejmującym źródła pakietu, mapowy pin oraz pochodne. Pozostałe zmiany `map_manifest.json`, `UnderwaterMap.tscn`, metadanych builda i `structures/*/generated/**` należą do zwykłego zakresu Mapy. Przy kilku nowych sealed manifestach builder może odświeżyć dokładne SHA-256 jednym batchem i musi pozostawić spójny zestaw.
- Builder i runner odrzucają niezgodne EOL oraz izolują `.godot`, `user://`, logi i porty. Zatwierdzona kolejność to `implementacja -> lokalne testy zadania -> lokalny fast-check`; po `PASS` następuje `commit -> push + PR -> KONIEC`. Osobny wymagany GitHub `fast-check PASS` oraz pełne testy złożenia w merge queue przed scaleniem działają zgodnie z ARD-0113; stan finalnego buildera opisuje globalny `.ai/PROJECT_CONTEXT.md`.

## Luki

- `[PENDING_BUILDER_COPY]` Aktywne komunikaty help/error buildera nadal używają historycznej terminologii osobnego hand-offu. Obowiązujący kontrakt wymaga jednego root-routed PR seal+pin; uproszczenie tekstu w kodzie należy do osobnego map-owned PR.
- `[PENDING_REAL_DIVER_ROUTE]` Nowa pętla S ma dwukierunkowy headless route replay na produkcyjnym rastrze nawigacji, ale przekrojowy replay produkcyjnym Nurkiem i jego kapsułą należy do Root/integration. Musi zostać wykonany po scaleniu tej mapowej zmiany ze świeżego `main`; lokalny test Mapy nie może składać Mapy z Nurkiem.
- `[PENDING_USER_ACCEPTANCE]` Automatyczny capture nie zastępuje ręcznego przepłynięcia tras, odbioru skali, czytelności wejść, kolizji Nurka, atmosfery ani wydajności pełnego `DiveScene`.
- `[PENDING_PERFORMANCE_ACCEPTANCE]` Rezydencja L01/L02 przechodzi test skrajnych pozycji, stale requestów i budżetu, lecz końcowe hitching/VRAM należy jeszcze potwierdzić w ręcznym przebiegu pełnego `DiveScene`.

## Ostatnia weryfikacja

- 2026-09-01: refresh/build/check odtworzyły gameplayową L05 z SHA `a34159b0…59676`, topologią `topology-v1:30f3e100…dedc3`, partycją `partition-v1:1851a8f7…b22a` i sygnaturą `manifest-v2:a485ff84…`; portal clearance przeszedł `6/6`. Map smoke, `s_loop_gameplay_test.gd`, visual residency, boundary, campaign map/format, tutorial partial-loot oraz testy current/threat/rescue/buoy/heavy/diver-clearance przeszły bez `ERROR` i `SCRIPT ERROR`; lokalny test pętli obejmuje rzeczywiste `current_at()`, instancjonowanie gameplayu oraz route replay tutorialu, Archive, R3, obu tras C4, obu skrótów i salvage w obie strony.
- Natywny survey przeszedł `1/1 PASS` z receiptem `63625d7c…beef`, workspace zachowano w `target_000_13968_8e1c507ac0cb485fbf89576d37dccf03`, a raport w `run_20260901_153116_742_13968_a5a4e274d2e04ac29349ef0c01856a07`. Telemetria potwierdziła `missing=0`, `failed=0`, settled render i szczyt `17 923 287` pikseli; oględziny overview oraz Station/Archive/R3/C4, safe/risk/salvage i obu stron trzech portali potwierdziły spójną masę skały oraz otwartą wodę na przejściach. To techniczny capture, nie ręczne przepłynięcie pełnego `DiveScene`.
- 2026-09-01: kanoniczny build i niedestrukcyjny check odtworzyły presentation v27 (`presentation-v2:18488946…f29ad3`) z manifestu `4660f543…3efe3e`; sygnatura gameplayu pozostała dokładnie `manifest-v2:fbde2d70…b17325`. Map smoke (`b68cea60…b1a1c2`), rezydencja L01/L02 (`4042b27a…cca54a`), boundary (`54c1eb0d…56866b`) i portal backdrop clearance `6/6` przeszły bez `ERROR` i `SCRIPT ERROR`.
- Natywny `underwater_map_proxy_capture_test.gd` przeszedł `1/1 PASS` z receiptem `8bede5fd…13329e`, zachowanym workspace i surveyem `run_20260901_141509_645_31604_f3549064fe4a4569aaef7fa3e739cffe`. Telemetria końcowa potwierdziła `missing=0`, `failed=0`, `overcommit=0`, szczyt `17 923 287 / 48 000 000` pikseli oraz 47 deskryptorów/17 ścieżek. Oględziny overview i kadrów landmarków przy gameplay zoom `1.2` potwierdziły spokojną L01, odrębne dominanty R3/Station/Archive/C4, przewagę prawdziwych struktur i L05 nad tłem oraz brak fałszywej kolizji, wejścia i platformy. Pięć nowych kandydatów ImageGen pozostało poza manifestem i diffem binarnym po odrzuceniu za nadmierną ruinę, perspektywę, fałszywe affordance albo niespełniony kontrakt alfy.
- 2026-08-29: kanoniczny build i niedestrukcyjny check odtworzyły presentation v24 (`presentation-v2:8ab82737…f19f7844`). Map smoke, pełny test rezydencji L01/L02 i osobny fast-check przeszły jako PASS; rezydencja zachowała pełne 228 bindingów, 40 unikalnych tekstur i niezmieniony budżet.
- Natywny `underwater_map_proxy_capture_test.gd` przeszedł `1/1 PASS` z zachowaniem workspace. Oględziny overview, wszystkich 149 kadrów `target_0000–0148` i dziesięciu najrzadszych ujęć potwierdziły brak pustego L00 oraz brak kurtyny i fałszywej geometrii gameplay; minimum rzeczywistej zawartości kadru wyniosło `3,582%`, a p5 około `5,000%`.
- 2026-08-29: W01 przyjął sealed manifest `f3791ecc…f3a13`; celowany build/check pakietu oraz pełny mapowy `--build` i niedestrukcyjny `--check` odtworzyły presentation v20 (`presentation-v2:c334f393…7c835`) bez zmiany lokalnej lub globalnej topologii, partycji ownerów ani sygnatury gameplayu.
- Portal contract przeszedł `6/6`. Kontrakt i runtime W01, map smoke, boundary, publiczne osadzenie struktury oraz clearance Nurka przeszły po `1/1 PASS`, bez `ERROR` i `SCRIPT ERROR`. Generator L04 odtworzył byte-identyczny asset `e0dde253…cd612`; bitmapa ma `2240 × 3680`, binarną alfę zgodną z maską, brak rozbieżności i brak RGB pod alfą zero.
- Natywny capture W01 oraz świeży survey prawdziwego `UnderwaterMap.tscn` przeszły po `1/1 PASS`. Oględziny pełnej struktury, entry, B/C/D, szybu, piwnicy, drzwi w ruchu, overview i obu podejść `target_0020/0021` potwierdziły czytelną otwartą wodę, konstrukcję wyłącznie na solid oraz brak dawnego ciemnego prostokąta i halo przy wejściu.
- Capture dowodzi rzeczywistego renderu oraz stanów runtime, lecz nie zastępuje ręcznego przepłynięcia ani odbioru wydajności pełnego `DiveScene`.
